/**
 * @organ core
 * @tissue types
 * @description Shared TypeScript types intended for both Convex backend and Next.js frontend.
 *   Keep this file small; only add types that truly must be shared.
 * @depends-on
 *   - (none)
 * @depended-by
 *   - Frontend and backend code that imports shared domain types
 */

// Placeholder for shared types (Phase 0.1 scaffold).
