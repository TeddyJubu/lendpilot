# M0.3 Re-verifier — scope-corrected acceptance pass (Verifier)

## Scope (M0.3 only)
Verify ONLY:
- Convex initialized under `apps/web`
- Baseline schema/config files exist
- Root/workspace scripts coherent for app/worker/tests
- Env templates + `.gitignore` cover local setup
- Baseline verification commands succeed

## Evidence
### Convex initialized under `apps/web`
- Present: `apps/web/convex.json`
- Present: `apps/web/convex/` with `schema.ts`, `types.ts`, `http.ts`, `crons.ts`, `core/tables.ts`
- Note: `apps/web/convex/_generated/` not present locally (Convex codegen/dev not run in this workspace)

### Baseline scripts / monorepo wiring
- Root `package.json` exists with scripts:
  - `web:*` scripts delegate to `pnpm -C apps/web ...`
  - `worker:*` scripts delegate to `npm --prefix services/mortgage-data-engine ...`
  - root `typecheck` and `test` run both web + worker
- Root `pnpm-workspace.yaml` and `pnpm-lock.yaml` present

### Env templates + ignore rules
- Present: root `.env.example`
- Present: `apps/web/.env.local.example`
- Present: `services/mortgage-data-engine/.dev.vars.example`
- Root `.gitignore` ignores `.env*`, `.dev.vars`, build outputs, Convex working dirs

## Verification commands run (from repo root)
- `pnpm web:lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm web:build`

Result: all exit code 0.

## Notes
- `pnpm convex:dev` / `npx convex dev` not run (interactive first-run auth + project provisioning). For M0.3 scope this is treated as a NOTE, not a blocker.
- Searched for note IDs `9cbb8513-...` and `b046e53c-...` in-repo; not found.

