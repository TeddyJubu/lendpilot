# M0.4 Verifier Preflight — design-system base

## Status
Preflight only (no implementation). Evidence gathered + acceptance checklist + smallest safe command plan.

## Scope (M0.4 only)
Verify ONLY design-system base concerns:
- Tailwind v4 token foundation + OKLCH CSS vars
- shadcn/ui base (Layer 1) installed under `apps/web/src/components/ui/`
- LoanPilot primitives (Layer 2) implemented under `apps/web/src/components/primitives/`
- Dark mode support via `next-themes` (attribute="class")
- Test coverage for primitives (Vitest + RTL)

Explicitly OUT of scope: Clerk/auth, Convex data correctness, app shell completion, view routing/features.

## Evidence (current repo state)
### Token foundation
- Present: `apps/web/src/styles/globals.css` with `@import "tailwindcss"`, `@import "tw-animate-css"`, `:root` tokens, `.dark` overrides, and `@theme inline` bindings.
- Present: `apps/web/src/app/layout.tsx` imports `../styles/globals.css` and loads Geist fonts.

### shadcn + primitives
- `apps/web/src/components/ui/` exists but is **empty**.
- `apps/web/src/components/primitives/` exists but is **empty**.
- No `components.json` (shadcn config) found in-repo (expected after `shadcn init`).

### Dark mode
- Tokens support `.dark`, but **no ThemeProvider / next-themes integration** present in `layout.tsx`.
- `next-themes` is not present in `apps/web/package.json` deps.

### Tests / quality signals
- Commands run (from repo root): `pnpm -C apps/web lint && pnpm -C apps/web typecheck && pnpm -C apps/web test && pnpm -C apps/web build`
  - Exit code 0.
  - Vitest reports: **No test files found** (expected given empty primitives).

## M0.4 acceptance checklist (design-system base)
Source anchors: `IMPLEMENTATION.md` Phase 0.2 + `DESIGN_SYSTEM.md`.

### A) Tokens (Tailwind v4)
- [ ] `apps/web/src/styles/globals.css` matches DESIGN_SYSTEM.md token set (light + dark), including status/priority/stage tokens.
- [ ] `@theme inline` binds every token used by utilities (no missing `--color-*`).
- [ ] No raw Tailwind palette colors used in primitives/features (e.g. `red-500`, `blue-600`).

### B) shadcn Layer 1 (installed, unmodified)
- [ ] `apps/web/src/components/ui/*` populated via shadcn for Phase 0 baseline components (DESIGN_SYSTEM.md Section 11).
- [ ] shadcn config exists (`components.json`) with Radix + Zinc + CSS variables.
- [ ] `components/ui/` files remain generator-owned (no manual edits).

### C) Primitives Layer 2 (12 required)
- [ ] All 12 primitives exist per `IMPLEMENTATION.md` Phase 0.2:
  `status-badge`, `priority-indicator`, `stage-pill`, `user-avatar`, `empty-state`,
  `ai-content`, `time-ago`, `keyboard-shortcut`, `loading-state`, `metric-card`,
  `detail-panel`, `confirm-dialog`.
- [ ] Each primitive uses design tokens + shadcn components; no inline styles.

### D) Dark mode
- [ ] `next-themes` integrated with `attribute="class"` and default `system`.
- [ ] A toggle exists (can be a minimal DS test control) to switch light/dark.
- [ ] All primitives visually correct in both modes (manual spot-check on a gallery page).

### E) Tests (per primitive)
- [ ] A component test file exists for each primitive.
- [ ] Tests cover: renders, variant/prop behavior, and at least one interaction when applicable.
- [ ] `pnpm -C apps/web test` is green and includes >0 tests.

### F) Demo / gallery page
- [ ] A dedicated “design system” test page exists that renders:
  - shadcn base components (smoke)
  - all primitives with their variants
  - a theme toggle to verify dark mode

## Smallest safe verification command plan (no installs)
1) Static checks (from repo root):
- `pnpm -C apps/web lint`
- `pnpm -C apps/web typecheck`
- `pnpm -C apps/web test`
- `pnpm -C apps/web build`

2) Manual verification (from `apps/web`):
- `pnpm dev`
- Open DS gallery route; toggle theme; visually confirm all primitives render correctly.
- Check browser console: no errors.

3) Optional targeted greps (post-implementation):
- Search for raw palette colors in primitives: `rg "(red|blue|green|zinc|slate)-[0-9]{3}" apps/web/src/components/primitives`

## Likely over-scope traps
- Implementing Phase 0.3 shell/auth tasks (Clerk, sidebar, command bar behavior) during M0.4.
- Adding Convex/organs logic or data wiring to “demonstrate” primitives.
- Editing shadcn `components/ui/*` files directly instead of wrapping in primitives.
- Introducing raw Tailwind colors or inline styles that bypass tokens.

## Preflight verdict
NOT READY for M0.4 acceptance yet: token foundation exists, but shadcn Layer 1, primitives Layer 2, next-themes dark mode, and primitive test coverage are currently missing.
