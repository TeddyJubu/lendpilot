# M0.4 Final Verifier — strict design-system acceptance pass

## Scope
M0.4 only. Verify ONLY:
- shadcn Layer 1 presence
- documented Layer 2 primitive coverage
- dark-mode foundation
- design-system gallery route
- primitive/component test coverage (smallest strict command set)

Out of scope: Clerk/auth, Convex data organs, shell completion, Cmd+K/copilot behavior.

## Required inputs read
- `CLAUDE.md`, `IMPLEMENTATION.md`, `DESIGN_SYSTEM.md`
- Verifier preflight note: `docs/task-notes/6ad48e84-de9c-49b4-940e-a9cde49794d5.md`
- Retry implementation note: `docs/task-notes/effb70ed-564f-4ca5-8f2e-36963438979a.md`

### Missing in repo
- Implementor preflight note ID `9b3f170a-cc1e-43f7-bef5-8846feca54d3` not found via ripgrep across repo.
- A standalone "current spec" file/folder was not found; acceptance anchors appear to be `IMPLEMENTATION.md` Phase 0.2 + `DESIGN_SYSTEM.md`.

## Evidence (repo state)
### A) shadcn Layer 1 present
- `apps/web/src/components/ui/` exists and contains shadcn components (e.g. `button.tsx`, `card.tsx`, `dialog.tsx`, `sheet.tsx`, `sidebar.tsx`, etc.).
- shadcn config present: `apps/web/components.json`.

### B) Layer 2 primitives present + documented coverage
- `apps/web/src/components/primitives/` contains 12 primitives:
  `keyboard-shortcut`, `time-ago`, `user-avatar`, `status-badge`, `priority-indicator`, `stage-pill`,
  `metric-card`, `empty-state`, `ai-content`, `loading-state`, `detail-panel`, `confirm-dialog`.
- Documentation anchors:
  - `DESIGN_SYSTEM.md` Section 6 documents the primitive set + intended props/behavior.
  - `apps/web/src/components/README.md` documents the 3-layer architecture.

### C) Dark mode foundation
- Theme provider: `apps/web/src/components/shell/theme-provider.tsx` uses `next-themes`.
- App integration: `apps/web/src/app/layout.tsx` wraps content with
  `ThemeProvider attribute="class" defaultTheme="system" enableSystem`.
- Tokens include `.dark { ... }`: `apps/web/src/styles/globals.css`.

### D) Design-system gallery route
- Route exists: `apps/web/src/app/design-system/page.tsx`.
- Next build output includes `/design-system` route.

### E) Primitive/component test coverage
- One test file per primitive exists under `apps/web/src/components/primitives/__tests__/` (12 files).

## Verification commands run (smallest strict set)
From repo root:
- `pnpm -C apps/web typecheck` ✅
- `pnpm -C apps/web test` ✅ (12 files, 18 tests)
- `pnpm -C apps/web build` ✅ (route table includes `/design-system`)

## Verdict
PASS (meets M0.4 criteria). Notes: implementor preflight note UUID not present in repo; no separate spec file located beyond the implementation/design-system docs.

