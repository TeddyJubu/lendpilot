# M0.2 — Web app scaffold (`apps/web`)

## Status
Complete.

## Deliverables
- Scaffolded `apps/web` as Next.js **15** App Router + TypeScript (strict) project.
- Added Tailwind v4 design-token foundation at `apps/web/src/styles/globals.css` (per `DESIGN_SYSTEM.md`).
- Added minimal organ-oriented folder structure under `apps/web/src/components/*` (placeholders only).
- Added Vitest config + scripts (`pnpm test` passes with no tests).
- Added placeholder `apps/web/convex/README.md` to reserve backend co-location.
- Replaced create-next-app template README with a LoanPilot-oriented README and removed unused template public assets.

## Verification Run
From `apps/web`:
- `pnpm typecheck`
- `pnpm lint`
- `pnpm test`
- `pnpm build`

## Notes / Follow-ons
- pnpm warned about ignored build scripts (`esbuild`). If Vitest later fails during real tests, run `pnpm approve-builds` and allow `esbuild`.
- `apps/web/src/app/icon.svg` is a lightweight placeholder app icon for the scaffold.
