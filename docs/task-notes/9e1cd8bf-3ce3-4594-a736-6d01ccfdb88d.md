# M0.3 — Verification Preflight (App Shell + Auth)

## Status
Preflight checklist + command plan only (no final acceptance verdict in this pass).

## Evidence snapshot (current repo state)
- Repo root contains: `apps/web`, `services/mortgage-data-engine`, `docs/`.
- **No root `package.json`** and **no root `pnpm-workspace.yaml`** detected.
- `apps/web/` exists with `pnpm-lock.yaml`, `package.json`, Next.js 15 scaffold.
- `apps/web/convex/` currently contains **only** a placeholder `README.md` (no Convex init yet).
- `services/mortgage-data-engine/` uses **npm** (has `package-lock.json`).
- Root `.gitignore` exists and ignores `**/node_modules/`, `**/dist/`, `**/.next/`, `.env*`.

## M0.3 Acceptance Criteria (what must be true)
Source: `IMPLEMENTATION.md` Phase 0.3 gate.

### Functional gate
- User can **sign up / sign in via Clerk**.
- Session persists across refresh.
- Signing in **creates/ensures a `users` record in Convex** (core upsert mutation).
- Shell renders with:
  - Sidebar (Today / Pipeline / Contacts + Settings)
  - Command bar skeleton (Cmd+K opens, groups render, Esc closes)
  - Copilot panel skeleton (Cmd+/ toggles, chat UI renders)
  - 3 view routes render empty states and navigation works.
- Keyboard shortcuts:
  - `1/2/3` navigate
  - `Cmd+K`, `Cmd+B`, `Cmd+/` toggle panels.
- All core organ tests pass.

### Technical gate (implicit for “acceptance”)
- `apps/web` builds, typechecks, lints, and tests green.
- Convex dev deployment connects and schema pushes.

## Official guidance anchors (for final verify)
- Convex CLI: `npx convex dev` creates `convex/` directory and writes `CONVEX_DEPLOYMENT` to `.env.local`; updates `convex/_generated`.
  - https://docs.convex.dev/cli
- Convex config: `convex.json` is expected in the **project root (same dir as package.json)**.
  - https://docs.convex.dev/production/project-configuration
- pnpm workspaces: a workspace must have `pnpm-workspace.yaml` in the **repository root**.
  - https://pnpm.io/workspaces

## Strict verification checklist (final pass)
### A) Repo / package-manager consistency
- [ ] Confirm intended package manager strategy:
  - Option 1: True monorepo (root `package.json` + root `pnpm-workspace.yaml` + single root lockfile)
  - Option 2: Multi-package but *not* a pnpm workspace (then remove/avoid “workspace” assumptions in docs/scripts).
- [ ] Ensure no mixed lockfiles unless explicitly justified (`pnpm-lock.yaml` vs `package-lock.json`).
- [ ] Ensure `node_modules/` and build outputs are not tracked.

### B) Convex init shape (apps/web)
- [ ] `apps/web/convex.json` present (or equivalent) and points to `convex/`.
- [ ] `apps/web/convex/` contains `schema.ts` and core organ tissues per CLAUDE.md/IMPLEMENTATION.md.
- [ ] `apps/web/convex/_generated/` exists and is committed (required for TS typecheck).
- [ ] `npx convex dev` succeeds from `apps/web`.

### C) Core organ (Convex)
- [ ] `core/tables.ts`: users + syncLog schema matches CLAUDE.md.
- [ ] `core/mutations.ts`: createOrGetUser (idempotent, sets `updatedAt`, correct owner/identity mapping).
- [ ] `core/queries.ts`: getCurrentUser.
- [ ] Tests exist under `apps/web/convex/core/__tests__/` and pass.

### D) Clerk auth integration
- [ ] Clerk provider wired in `apps/web/src/app/layout.tsx`.
- [ ] Middleware / route protection aligned with App Router.
- [ ] Env vars documented (template file) and no secrets committed.

### E) Shell + routing
- [ ] Sidebar renders and routes work.
- [ ] Command bar + copilot panel open/close reliably.
- [ ] Global keyboard handler does not conflict with form inputs.
- [ ] No console errors.

### F) Quality gates
- [ ] `pnpm lint` (0 warnings), `pnpm typecheck` (0 errors), `pnpm test` (green), `pnpm build` (green).

## Command plan (final verification pass)
All commands should be run with a clean working tree.

1) From `apps/web`:
- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`

2) Convex (from `apps/web`):
- `npx convex dev` (ensure schema pushes; `_generated` updates)

3) App runtime (from `apps/web`):
- `pnpm dev`
- Manual: sign up/in with Clerk → confirm user record exists in Convex (Dashboard Data page or `npx convex data users`).
- Manual: verify shortcuts + panels + routing + empty states.

## Risk list (likely failure points)
- **High:** Missing Convex init (`convex/`, `convex.json`, `_generated`) blocks all M0.3 auth/data gates.
- **High:** No root pnpm workspace config; scripts/docs may assume monorepo recursion that won’t work.
- **High:** Mixed package managers/lockfiles (pnpm in web, npm in worker) can cause drift and CI inconsistency.
- **Medium:** Env templates absent → hard to verify Clerk/Convex setup without tribal knowledge.
- **Medium:** Ignore coverage: ensure `.env.local` created by Convex remains uncommitted; ensure generated artifacts policy is clear (Convex `_generated` should be committed).

