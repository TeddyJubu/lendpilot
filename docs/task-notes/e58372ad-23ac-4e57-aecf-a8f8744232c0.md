# M0.5 Implementor — auth + shell skeleton

Task Note ID: `e58372ad-23ac-4e57-aecf-a8f8744232c0`

## Scope (Phase 0.3 only)
- Clerk auth wiring (App Router)
- Convex current-user bootstrap (create/ensure `users` record)
- Shell skeleton routes/surfaces (Today/Pipeline/Contacts/Settings)
- Global keyboard shortcuts (Cmd+K, Cmd+/, Cmd+B, 1/2/3, Esc, ?)
- Tests + smallest relevant verification commands

Out of scope: any real CRM feature implementation beyond placeholders/empty states.

## Inputs / Notes requested
- Implementor preflight note `23ae1a67-1f7c-4ebf-8c45-69bfcd2970fb`: **not found in repo** via code search.
- Verifier preflight note `d473aa47-7851-469f-a3cf-12c891ed2f62`: **not found in repo** via code search.
- “Current spec” file/folder: **not found** as a standalone artifact; Phase 0.3 acceptance anchors appear to be `IMPLEMENTATION.md` + `DESIGN_SYSTEM.md` + `CLAUDE.md`.

## Progress

### Convex: core user bootstrap tissue (implemented)
Added:
- `apps/web/convex/core/mutations.ts` — `createOrGetUser`
  - Auth via `ctx.auth.getUserIdentity()`
  - Idempotent by `identity.subject` → `users.clerkId`
  - Inserts default `tier: "trial"`, `onboardingCompleted: false`
  - Patches email/name on subsequent sign-ins
  - Always sets `updatedAt: Date.now()`
- `apps/web/convex/core/queries.ts` — `getCurrentUser`
  - Returns `null` if unauthenticated or user row missing
  - Returns minimal shape for shell bootstrap

### Convex tests (added; import paths corrected)
Added (Vitest + convex-test):
- `apps/web/convex/core/__tests__/mutations.test.ts`
- `apps/web/convex/core/__tests__/queries.test.ts`

These tests depend on Convex codegen output (`apps/web/convex/_generated/*`).

### Dependencies
- Added `convex-test` (devDependency) in `apps/web`.

## Blockers / Known issues
### Convex codegen requires an initialized deployment
Attempted:
- `pnpm -C apps/web convex:codegen`

Result:
- Fails with: `No CONVEX_DEPLOYMENT set, run \`npx convex dev\` to configure a Convex project`.

Impact:
- `apps/web/convex/_generated/` is currently missing in this workspace.
- Until `convex dev` is run once (or `CONVEX_DEPLOYMENT` + `NEXT_PUBLIC_CONVEX_URL` are provided), TypeScript/typecheck and convex-test compilation will fail.

## Next steps (remaining Phase 0.3 work)
1) Run `pnpm -C apps/web convex:dev` once (interactive first-run) to generate `_generated/`.
2) Wire Clerk + Convex providers + route protection.
3) Implement shell skeleton + shortcut handling + placeholder routes.
4) Run verification: `pnpm -C apps/web lint/typecheck/test/build`.
